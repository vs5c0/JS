//bitwise Operator--> used to perform the binary Operations(0's,1's)
//There are mainly 2 types . They are
// 1 Bitwise AND
let form = 3;
let vorm = 2;
let cv = (form & vorm)
console.log("Bitwise AND: ", cv)

// 2 Bitwise OR
let formm = 3;
let vormm = 2;
let cvm = (form | vorm)
console.log("Bitwise OR: ", cvm)

// Operator Precendence 
//we can use the complex problems by using ()

let jh = (10+(20*4)+20-5+(20**2));
console.log("Operator Precedence: ",jh)