// Arrays --> are usesd to store the Simailar type if data
// used to store the similar type of data
// Index plays a keyrole to access and modify the data

//Access the data

let names = ['Ram', 'Vikram','Pavan']

console.log(names[0]);

// Modify the data or change the data

let Countries = ['Bharat','America','India','Ukraine']
console.log(Countries);

//after adding the data based on index
Countries[3] = 'Austria'
console.log(Countries);