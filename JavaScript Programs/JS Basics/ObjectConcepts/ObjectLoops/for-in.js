// fro in loop is used to iteration of the code
//keys and let keyword plays an imp role

let square={
    length:23,
    breadth:32
};
for(let key in square){
    console.log(key,square[key])
}